@import url(./switch-button.postcss);

/*
    避免 gulp 多次重复打包，在入口文件处只引入一次
    @import url(/style/common/modal/index.postcss);
*/

#loginModal #loginForm .reset-pwd-link {
    height: 25px;
    line-height: 25px;
    font-size: 0.75em;
    float: right;
}
