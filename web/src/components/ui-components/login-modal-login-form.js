'use strict';

const React = require('react');

class LoginForm extends React.Component {
    render() {
        return (
            <form id="loginForm">
                登录表单
            </form>
        );
    }
}

module.exports = LoginForm;
