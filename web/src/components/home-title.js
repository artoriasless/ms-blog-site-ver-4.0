'use strict';

const React = require('react');

class HomeTitle extends React.Component {
    render() {
        return (
            <h4>test home page title</h4>
        );
    }
}

module.exports = HomeTitle;
